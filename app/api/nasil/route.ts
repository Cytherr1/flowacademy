export function GET(request: Request) {
	return new Response('Ayrı bir backend yok, backendde kullanılacak fonksiyonlar bu formatta yazılacak.');
}

// npm run dev yaptıktan sonra http://localhost:3000/api/nasil request atıp nasıl çalıştığına bakabilirsiniz.